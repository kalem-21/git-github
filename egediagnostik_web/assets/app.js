const pageStyles=document.createElement('link');pageStyles.rel='stylesheet';pageStyles.href='/assets/pages.css';if(!document.querySelector('link[href="/assets/pages.css"]'))document.head.appendChild(pageStyles);
const body=document.body,menu=document.querySelector('.menu');if(menu){menu.onclick=null;menu.addEventListener('click',()=>body.classList.toggle('navopen'))}
document.querySelectorAll('.navitem>a').forEach(link=>link.addEventListener('click',e=>{if(innerWidth<=840&&link.parentElement.querySelector('.dropdown')){e.preventDefault();const item=link.parentElement;document.querySelectorAll('.navitem.open').forEach(x=>x!==item&&x.classList.remove('open'));item.classList.toggle('open')}}));
document.addEventListener('click',e=>{if(innerWidth<=840&&!e.target.closest('.top'))body.classList.remove('navopen');if(e.target.closest('.mainnav a:not(.navitem>a)'))body.classList.remove('navopen')});addEventListener('resize',()=>{if(innerWidth>840){body.classList.remove('navopen');document.querySelectorAll('.navitem.open').forEach(x=>x.classList.remove('open'))}});
const progress=document.querySelector('#scrollProgress');addEventListener('scroll',()=>{if(progress){const h=document.documentElement.scrollHeight-innerHeight;progress.style.width=(h?scrollY/h*100:0)+'%'}} ,{passive:true});
const reveals=[...document.querySelectorAll('.reveal')];if('IntersectionObserver'in window){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}}),{threshold:.12});reveals.forEach(x=>io.observe(x))}else reveals.forEach(x=>x.classList.add('is-visible'));
function updateTime(){const d=new Date(),clock=d.toLocaleTimeString('tr-TR',{hour12:false}),day=d.getDay(),m=d.getHours()*60+d.getMinutes(),open=day>=1&&day<=5&&m>=510&&m<1080;document.querySelectorAll('#liveClock,#clockLarge').forEach(x=>x.textContent=clock);const txt=open?'AÇIK • 08:30–18:00':'KAPALI • Sonraki iş günü 08:30';const w=document.querySelector('#workState'),wl=document.querySelector('#workLarge');if(w)w.textContent=txt;if(wl){wl.textContent=open?'AÇIK':'KAPALI';wl.style.color=open?'#118d67':'#b85c5c'}}updateTime();setInterval(updateTime,1000);
const slides=[...document.querySelectorAll('.slide')],dotsWrap=document.querySelector('.slide-dots');let current=0,sliderTimer;if(slides.length&&dotsWrap){slides.forEach((_,i)=>{const b=document.createElement('button');b.type='button';b.setAttribute('aria-label',(i+1)+'. slayt');b.onclick=()=>go(i,true);dotsWrap.appendChild(b)});const dots=[...dotsWrap.children];function render(){slides.forEach((s,i)=>s.classList.toggle('active',i===current));dots.forEach((d,i)=>d.classList.toggle('active',i===current))}function go(i,manual=false){current=(i+slides.length)%slides.length;render();if(manual)restart()}function restart(){clearInterval(sliderTimer);sliderTimer=setInterval(()=>go(current+1),6500)}document.querySelector('.slide-arrow.prev')?.addEventListener('click',()=>go(current-1,true));document.querySelector('.slide-arrow.next')?.addEventListener('click',()=>go(current+1,true));render();restart();document.querySelector('.slider')?.addEventListener('mouseenter',()=>clearInterval(sliderTimer));document.querySelector('.slider')?.addEventListener('mouseleave',restart)}
const cf=document.querySelector('#contactForm'),fs=document.querySelector('#formStatus');if(cf)cf.addEventListener('submit',async e=>{e.preventDefault();if(fs)fs.textContent='Gönderiliyor…';const body=Object.fromEntries(new FormData(cf));try{const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}),j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||'error');if(fs)fs.textContent='Mesajınız alındı. Teşekkür ederiz.';cf.reset()}catch{if(fs)fs.textContent='Mesaj şu anda gönderilemedi. Lütfen e-posta ile iletişime geçin.'}});
if(matchMedia('(pointer:fine)').matches){document.querySelectorAll('.principles article,.solution-grid article,.service-grid article,.news-grid article,.code-card').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${(-y*2.5).toFixed(2)}deg) rotateY(${(x*3).toFixed(2)}deg) translateY(-4px)`});card.addEventListener('pointerleave',()=>card.style.transform='')})}